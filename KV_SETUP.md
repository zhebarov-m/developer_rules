# 🚀 Настройка глобальной статистики на Vercel

## Текущая версия (In-Memory)

Сейчас используется in-memory хранилище, которое:
- ✅ Работает на Vercel
- ❌ Сбрасывается при перезапуске сервера
- ❌ Не подходит для production с большим трафиком

## 🔧 Настройка Vercel KV для постоянного хранения

### Шаг 1: Создайте KV Database в Vercel

1. Зайдите в Vercel Dashboard
2. Выберите ваш проект
3. Перейдите в **Storage** → **Create Database**
4. Выберите **KV** (Key-Value)
5. Создайте базу данных

### Шаг 2: Установите пакет

```bash
npm install @vercel/kv
```

### Шаг 3: Обновите API routes

Раскомментируйте код с KV в файлах:
- `api/stats/visit/index.ts`
- `api/stats/like/index.ts`

### Шаг 4: Добавьте переменные окружения

Vercel автоматически добавит переменные окружения для KV после создания базы.

### Шаг 5: Деплой

```bash
vercel --prod
```

## 📊 Как это работает

### Счетчик посещений
- Каждый заход на сайт → `POST /api/stats/visit`
- Увеличивает глобальный счетчик
- Все пользователи видят одно число

### Лайки
- Идентификация по IP адресу
- Один IP = один лайк
- Все лайки суммируются глобально
- Каждый пользователь видит общее количество

## 🔒 Безопасность

- IP адрес используется только для идентификации лайков
- Данные не содержат персональной информации
- CORS настроен для работы с фронтендом

## 📝 Примечания

- **In-memory**: Работает, но сбрасывается при перезапуске
- **Vercel KV**: Постоянное хранилище, рекомендуется для production
- **Rate Limiting**: Можно добавить через Vercel Edge Config или middleware

