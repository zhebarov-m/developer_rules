# 📚 Almanac - Руководство по написанию кода

Современное веб-приложение с правилами разработки для Frontend и Backend разработчиков.

## 🎯 Особенности

- ✅ **Два режима**: Frontend и Backend правила
- ✅ **Реальная статистика**: счетчик посещений и лайки через API
- ✅ **Красивый UI**: с акцентными цветами для каждого режима
- ✅ **Поиск**: быстрый поиск по всему контенту
- ✅ **Навигация**: удобный sidebar с разделами
- ✅ **Подсветка кода**: примеры кода с IDE-подобным отображением

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev
```

### Деплой на Vercel

1. **Через Vercel CLI:**
```bash
npm i -g vercel
vercel
```

2. **Через GitHub:**
   - Загрузите проект в GitHub
   - Подключите репозиторий к Vercel
   - Vercel автоматически определит настройки

## 📡 API Endpoints

### Счетчик посещений
- `GET /api/stats/visit` - получить количество посещений
- `POST /api/stats/visit` - увеличить счетчик

### Лайки
- `GET /api/stats/like` - получить статистику лайков
- `POST /api/stats/like` - добавить/убрать лайк
  ```json
  {
    "action": "add" // или "remove"
  }
  ```

## 💾 Хранение данных

### Текущая версия (Dev)
Используется in-memory хранилище, которое сбрасывается при перезапуске.

### Production (рекомендуется)
Для постоянного хранения данных используйте **Vercel KV**:

1. В Vercel Dashboard создайте KV Database
2. Установите пакет:
```bash
npm install @vercel/kv
```

3. Обновите API routes:
```typescript
import { kv } from '@vercel/kv';

// Вместо in-memory
await kv.incr('visitCount');
await kv.sadd('likes', clientId);
```

## 🎨 Цвета

- **Frontend**: `#2A4B7C` (синий)
- **Backend**: `#E34F27` (оранжевый)

## 📁 Структура проекта

```
dev-rules/
├── api/                    # Vercel Serverless Functions
│   └── stats/
│       ├── visit/         # Счетчик посещений
│       └── like/          # Лайки
├── src/
│   ├── app/               # Инициализация
│   ├── pages/             # Страницы
│   ├── widgets/           # Композитные блоки
│   ├── features/          # Фичи
│   ├── entities/          # Сущности
│   └── shared/            # Переиспользуемый код
├── public/
│   └── logo/              # Логотипы
├── DEVELOPMENT_RULES.md   # Правила для Frontend
└── DEVELOPMENT_RULES (B).md # Правила для Backend
```

## 🔧 Технологии

- **Frontend**: React 18 + TypeScript + Vite
- **Styling**: Tailwind CSS
- **Backend**: Vercel Serverless Functions
- **Storage**: In-memory (dev) / Vercel KV (production)

## 📝 Примечания

- API routes автоматически деплоятся как Serverless Functions на Vercel
- Для production рекомендуется использовать Vercel KV для постоянного хранения
- Все данные идентифицируются по IP адресу пользователя
