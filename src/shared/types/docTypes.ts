export type DocType = 'frontend' | 'backend';

export const DOC_TYPES = {
  FRONTEND: 'frontend' as DocType,
  BACKEND: 'backend' as DocType,
};

export const DOC_LABELS = {
  frontend: 'Frontend',
  backend: 'Backend',
};

export const DOC_DESCRIPTIONS = {
  frontend: 'Правила разработки Frontend',
  backend: 'Правила разработки Backend',
};

