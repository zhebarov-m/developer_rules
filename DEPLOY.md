# Vercel Deployment Guide

## 🚀 Деплой на Vercel

### 1. Установка зависимостей
```bash
npm install
```

### 2. Локальная разработка
```bash
npm run dev
```

### 3. Деплой на Vercel

#### Вариант 1: Через Vercel CLI
```bash
npm i -g vercel
vercel
```

#### Вариант 2: Через GitHub
1. Загрузите проект в GitHub
2. Подключите репозиторий к Vercel
3. Vercel автоматически определит настройки

### 4. Настройка переменных окружения (опционально)

Для production можно использовать Vercel KV для постоянного хранения:

1. Установите Vercel KV:
```bash
npm install @vercel/kv
```

2. В Vercel Dashboard добавьте KV Database
3. Обновите API routes для использования KV вместо in-memory хранилища

### 📝 Примечания

- **In-memory хранилище**: Текущая версия использует in-memory хранилище, которое сбрасывается при перезапуске
- **Vercel KV**: Для production рекомендуется использовать Vercel KV для постоянного хранения данных
- **API Routes**: Все API routes находятся в папке `/api` и автоматически деплоятся как Serverless Functions

### 🔧 Структура API

- `GET /api/stats/visit` - получить количество посещений
- `POST /api/stats/visit` - увеличить счетчик
- `GET /api/stats/like` - получить статистику лайков
- `POST /api/stats/like` - добавить/убрать лайк

